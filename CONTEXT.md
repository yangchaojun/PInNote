# PinNote

键盘优先的桌面便签应用；正在向"pin 窗口即全部界面"的形态改造（见 `.scratch/pin-only-map/map.md`）。

## Language

**Pin 窗口**:
一条笔记的无边框、置顶、跨 Space 桌面窗口；本形态下是应用唯一的界面（一条 live 笔记对应一个 Pin 窗口）。
_Avoid_: 桌面小部件、悬浮窗、预览窗

**收起**:
关闭 Pin 窗口的动作；笔记数据保留，下次启动随全部 live 笔记恢复窗口。
_Avoid_: 取消固定（已退役）、最小化

**事实源 Markdown**:
`note.content` 存储的 Markdown 文本——笔记内容的唯一权威表示；Pin 窗口的可编辑渲染视图只是它的一种呈现。
_Avoid_: 源文本、raw 模式

**整窗编辑**:
Pin 窗口打开即可直接输入、语法原地即时渲染的编辑形态（contenteditable / WYSIWYG）；不存在编辑与预览两种模式的切换。
_Avoid_: 实时预览、预览模式、编辑模式

**回收站**:
删除笔记后的 60 天保留区；本形态下无用户界面，仅由启动时的自动 purge 清理。
_Avoid_: 已删除笔记（作集合名）

**驻留**:
所有 Pin 窗口关闭后应用仍在后台运行（Dock/菜单栏可见），等待全局快捷键或重新打开窗口的状态。
_Avoid_: 最小化、托盘模式

**托盘图标**:
应用启动即出现在系统菜单栏/通知区域的单色小图标；左键唤起最新笔记（无笔记时新建一条），右键打开托盘菜单。是驻留状态的可见锚点与鼠标侧的唯一入口。
_Avoid_: 托盘模式、悬浮球

**托盘菜单**:
右键托盘图标弹出的系统上下文菜单：新建笔记 / 显示笔记列表 / 检查更新 / 退出。「显示全部笔记」（一次性开回全部窗口）已由「显示笔记列表」取代——回到笔记的方式是索引，不是把桌面铺满。退出为直接退出（退出时执行三层 flush），不弹确认框。发现新版本时多出一条「更新到 vX.Y.Z…」，图标同时换成彩色（角标）；不允许自更新的构建里该项退化为「打开下载页（.dmg）」。
_Avoid_: 主菜单、应用菜单（指 menu.go 的应用菜单时）

**更新卡片**:
托盘「检查更新」弹出的系统式更新窗口，由 Wails 框架自带（只改配色与文案覆写），承载 检查中 / 已最新 / 有新版 / 下载进度 / 待重启 / 出错 六个态与 Install、Skip、Remind later、Cancel、Restart & Apply 全套动作。应用不自建更新界面——pin-only 形态没有可挂横幅的主界面。
_Avoid_: 更新窗口（易与 pin 窗口混称）、升级对话框

**落库屏障**:
换包重启前的一道确认：Go 侧广播 `pin:flush-requested`，每个 Pin 窗口先把编辑器缓冲写回数据库再回执 `pin:flushed`；只要有一个窗口报脏或没回执，本轮更新直接作废。因为框架卡片的「Restart & Apply」无法被后端否决，屏障必须抢在下载/解包阶段做完。
_Avoid_: 保存检查、预检

**托盘面板**:
托盘菜单「显示笔记列表」打开的轻量索引窗口：列出全部 live 笔记（首行标题 + 摘要），点击一条即聚焦其 Pin 窗口并收起；无笔记时提供新建入口。失焦自动收起——它是索引，不是常驻界面，也不是主面板形态的回归。
_Avoid_: 主面板（退役概念）、托盘模式

**首行标题**:
`note.title` 的唯一产生规则——保存时从 content 第一个非空行派生（剥除 Markdown 记号），无独立可编辑标题；仅参与搜索与 header 悬停 tooltip。
_Avoid_: 笔记名、重命名

**降级块（rawSource）**:
审计未通过的 Markdown 块在编辑器中的保数据形态：以源码原样显示、原样写回，可手动「转为可编辑」。含行内图片等本期不支持的语法的块走此形态。
_Avoid_: 源码模式、raw 块

---

*已退役术语（改造前遗留，见 ADR-0001）：*

**固定 / 取消固定**（退役）:
主面板时代的动作；pin-only 形态下每条 live 笔记恒有窗口，「固定」失去语义。`Pinned` 字段与 `SetPinned` API 后端冻结保留。
_Avoid_: 在新代码与文档中使用

**未固定笔记**（退役）:
被「启动恢复全部 live 笔记窗口」取代——本形态下不存在没有窗口的 live 笔记。
